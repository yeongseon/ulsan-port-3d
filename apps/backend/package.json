{
  "name": "backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cd apps/backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "echo 'Use ruff check apps/backend'",
    "type-check": "echo 'Use mypy apps/backend'",
    "test": "echo 'Use pytest apps/backend'"
  }
}
